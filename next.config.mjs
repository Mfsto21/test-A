/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [{ protocol: "https", hostname: "**" }],
  },
  // Belt-and-suspenders alongside the robots metadata in layout.tsx — this
  // is a private client portal and should never be indexed, no matter how
  // a page is served (including routes the metadata API doesn't cover).
  async headers() {
    return [
      {
        source: "/:path*",
        headers: [{ key: "X-Robots-Tag", value: "noindex, nofollow" }],
      },
    ];
  },
};

export default nextConfig;
